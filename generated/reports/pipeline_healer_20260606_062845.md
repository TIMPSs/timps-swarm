# Pipeline Healer — 20260606_062845

**Classification:** linter_or_format (high confidence)  **CI:** github_actions  **ETA:** ~5m

## Root Cause
The CI configuration file '.github/workflows/timps-swarm.yml' is malformed/truncated, specifically in the 'outputs' section of the 'swarm' job. This prevents GitHub Actions from parsing the workflow definition.

**Correlated commit:** `N/A` — No recent commits provided, but the truncation likely occurred in the last commit that modified this file or during a file transfer/save operation.

## Evidence
- `    outputs:`
- `      run_id: ${{ steps.run.outputs.run_id }}`
- `      status: ${{ steps.wait.outpu`

## Fix patch
```diff
--- a/.github/workflows/timps-swarm.yml
+++ b/.github/workflows/timps-swarm.yml
@@ -59,4 +59,4 @@
   swarm:
     runs-on: ubuntu-latest
     outputs:
-      run_id: ${{ steps.run.outputs.run_id }}
-      status: ${{ steps.wait.outpu
+      run_id: ${{ steps.run.outputs.run_id }}
+      status: ${{ steps.wait.outputs.status }}
```

_Complete the truncated 'status' output line in the 'swarm' job's outputs. Assuming there's a step named 'wait' that produces a 'status' output, the correct syntax is 'status: ${{ steps.wait.outputs.status }}'._

## CI-config patch
```diff
--- a/.github/workflows/timps-swarm.yml
+++ b/.github/workflows/timps-swarm.yml
@@ -59,4 +59,4 @@
   swarm:
     runs-on: ubuntu-latest
     outputs:
-      run_id: ${{ steps.run.outputs.run_id }}
-      status: ${{ steps.wait.outpu
+      run_id: ${{ steps.run.outputs.run_id }}
+      status: ${{ steps.wait.outputs.status }}
```

## Retry strategy
- Should retry: **False**
- Max attempts: 1
- Backoff: 0s
- Conditions: This is a syntax error in the CI configuration itself, not a transient issue. Retrying will not resolve the problem and will only consume CI resources.

## Prevent Future
- Implement a pre-commit hook or a separate CI job that validates YAML syntax for all workflow files using a linter (e.g., `yamllint`).
- Ensure that file transfers or edits of CI configuration files are completed and saved correctly to prevent truncation.